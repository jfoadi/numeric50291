# Init file for the package
import numpy as np
import matplotlib.pyplot as plt
from simple_package.graphics import (histogram, display)
from simple_package.operations import interface
from simple_package.statistics import interface_statistics
from simple_package.operations import (add, subtract, multiply, divide, sine, cosine, tangent, power, factorial, log, log10, exp, sqrt)
from simple_package.statistics import (mean, median, std_dev, get_data)